[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma migrate deploy --schema server/prisma/schema.prisma && node server/dist/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
